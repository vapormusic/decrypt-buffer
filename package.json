{
  "name": "decrypt-buffer",
  "version": "1.1.0",
  "description": "Native NodeJS module to decrypt media in-memory",
  "main": "src/index.js",
  "scripts": {
    "rebuild": "cmake-js compile",
    "test": "standard && node test/tests.js",
    "prebuild": "prebuild --compile --strip --tag-prefix '' --backend cmake-js",
    "build": "run-script-os",
    "build:win32": "prebuild --platform win32 -r electron -t 30.0.4 --backend cmake-js && prebuild --platform win32 -r napi -t 6 --backend cmake-js",
    "build:linux": "prebuild --platform linux -r electron -t 30.0.4 --backend cmake-js && prebuild --platform linux -r napi -t 6 --backend cmake-js",
    "build:darwin": "prebuild --platform darwin -r electron -a x64 -t 30.0.4 --backend cmake-js && prebuild --platform darwin -r napi -a x64 -t 6 --backend cmake-js && prebuild --platform darwin -a arm64 -r electron -t 30.0.4 --backend cmake-js && prebuild --platform darwin -r napi -a arm64 -t 6 --backend cmake-js",
    "prebuild-install": "prebuild-install || (npm run rebuild || (true || VER>NUL))"
  },
  "keywords": [
    "mp4",
    "hls",
    "dash",
    "cenc",
    "native"
  ],
  "author": "vapormusic",
  "license": "GPL-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/vapormusic/decrypt-buffer.git"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "cmake-js": "^7.2.1",
    "node-addon-api": "^6.1.0",
    "prebuild": "^13.0.1",
    "prebuild-install": "^7.1.3",
    "run-script-os": "^1.1.6"
  },
  "devDependencies": {
    "mp4box": "^0.5.2",
    "standard": "^17.1.0"
  },
  "standard": {
    "ignore": [
      "Bento4"
    ]
  }
}
